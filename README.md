For making files, use the LAVGM program, then create an if statment inside the LAVGP file by changing the value stored into L to fit the max level count, and then recall each variable to make the if match the others.
